# Explain how connect yourself as new user.
# (With graphic session and ssh session)

Graphic session:

command:	su 'username'

SSH session:

1.	Connect to VM with ssh 'name'@'ip_of_VM'

Note: We always need user password or we can connect to root with root password